--- bin/portsclean.orig	2018-03-10 05:59:29.000000000 +1100
+++ bin/portsclean	2023-08-19 09:01:37.858485000 +1000
@@ -282,7 +282,7 @@
   msg = " --> Running ldconfig -m"
 
   dirs.each do |d|
-    dirs.delete(d) if ! (File.exists?(d) && File.stat(d).directory?)
+    dirs.delete(d) if ! (File.exist?(d) && File.stat(d).directory?)
   end
 
   case dirs.size
